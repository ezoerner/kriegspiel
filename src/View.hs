module View where

import           Helm.Engine.SDL (SDLEngine)

import           Helm.Graphics2D
import           Helm.Color
import           Model

overlay :: Color -> Model -> Form SDLEngine
overlay = undefined